# My [Prettier](https://prettier.io) config

## Usage

**Install**:

```bash
$ yarn add --dev antoine-prettier
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "antoine-prettier"
}
```
